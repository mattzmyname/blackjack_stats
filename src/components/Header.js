import React, {Component} from 'react';

export default class Header extends Component{
    render(){
        return(
            <div className="fixed-top card-header" style={{backgroundColor: '#37BC9B'}}>header</div>
        )
    }
}